<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= categoryName %><% if (subCategoryName) { %> - <%= subCategoryName %><% } %> | Plant Store</title>
  <link rel="stylesheet" href="/styles.css" />
  <style>
    body {
      font-family: 'Poppins', Arial, sans-serif;
      background: #f9f9f9; /* Match home page */
      color: #222;
      margin: 0;
      padding: 0;
      text-align: center;
    }
    header, footer {
      background:#4CAF50;
      color: white;
      padding: 15px;
      text-align: center;
    }
    footer p {
      margin: 0;
      font-size: 0.95rem;
    }
    .category-header {
      text-align: center;
      margin: 32px 0 18px 0;
    }
    .category-header h1 {
      color: #4CAF50;
      font-size: 2rem;
      margin-bottom: 8px;
    }
    .category-header p {
      color: #444;
      font-size: 1.1rem;
    }
    .search-bar-wrap {
      display: flex;
      justify-content: center;
      margin-bottom: 24px;
    }
    .search-bar-category {
      width: 320px;
      max-width: 90vw;
      padding: 10px 16px;
      border-radius: 24px;
      border: 1px solid #ddd;
      font-size: 1rem;
    }
    .category-box {
      display: grid;
      grid-template-columns: repeat(4, 1fr); /* 4 columns on desktop */
      gap: 20px;
      margin-top: 20px;
      justify-content: center;
    }
    .product-item {
      background: #fff;
      border: 1px solid #ddd;
      border-radius: 10px;
      padding: 15px;
      min-height: 340px;
      max-width: 260px;
      width: 100%;
      margin: 0 auto;
      text-align: center;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      box-shadow: 0 4px 6px rgba(0,0,0,0.08);
      transition: transform 0.3s, box-shadow 0.3s;
    }
    .product-item:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 18px rgba(52,152,219,0.13);
    }
    .product-item img {
      width: 100%;
      height: 120px;
      object-fit: cover;
      border-radius: 8px;
      background: #f9f9f9;
    }
    .product-item p {
      margin: 10px 0 6px 0;
      font-size: 1rem;
      color: #3498db;
      font-weight: 600;
    }
    .product-actions {
      display: flex;
      gap: 8px;
      justify-content: center;
      margin-top: 8px;
    }
    .product-actions a {
      flex: 1;
      padding: 7px 0;
      font-size: 0.97rem;
      text-align: center;
      border-radius: 5px;
      text-decoration: none;
      background: #3498db;
      color: #fff;
      transition: background 0.2s;
    }
    .product-actions a:hover {
      background: #217dbb;
    }
    @media (max-width: 900px) {
      .category-box { grid-template-columns: repeat(3, 1fr); }
    }
    @media (max-width: 600px) {
      .category-box { grid-template-columns: repeat(2, 1fr); } /* 2 columns on phone */
      .product-item { max-width: 100%; }
    }
    .no-categories {
      text-align: center;
      margin: 40px 0;
      color: #777;
    }
  </style>
</head>
<body>

  <header>
    <h1>Plant Store</h1>
    <!-- Add your navigation if needed -->
  </header>

  <main>
    <section class="category-header">
      <h1>
        <%= categoryName %>
        <% if (subCategoryName) { %>
          <span style="font-size:1.2rem;color:#3498db;">/ <%= subCategoryName %></span>
        <% } %>
      </h1>
      <% if (categoryDescription) { %>
        <p><%= categoryDescription %></p>
      <% } %>
    </section>

    <div class="search-bar-wrap">
      <input
        type="text"
        class="search-bar-category"
        id="categorySearch"
        placeholder="Search products in this category..."
        onkeyup="filterCategoryProducts()"
      />
    </div>

    <% if (products && products.length > 0) { %>
      <div class="category-box" id="categoryProductGrid">
        <% products.forEach(product => { %>
          <div class="product-item" data-name="<%= product.name.toLowerCase() %>">
            <img src="<%= (product.images && product.images[0]) ? product.images[0] : '/images/default-image.jpg' %>" alt="<%= product.name %>" />
            <p><strong><%= product.name %></strong></p>
            <p>₹<%= product.price.toFixed(2) %></p>
            <div class="product-actions">
              <a href="/api/products/productdetails?id=<%= product._id %>">View Details</a>
              <!-- <a href="javascript:void(0)" onclick="addToCart('<%= product._id %>', '<%= product.name.replace(/'/g, "\\'") %>', '<%= product.price %>', '<%= (product.images && product.images[0]) ? product.images[0] : '' %>', '<%= (product.description || '').replace(/'/g, "\\'") %>')">Add to Cart</a> -->
            </div>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <div class="no-categories">
        <h2>No Products Found</h2>
        <p>There are no products in this <%= subCategoryName ? 'subcategory' : 'category' %> yet.</p>
      </div>
    <% } %>
  </main>

  <footer>
    <p>&copy; <%= new Date().getFullYear() %> Plant Store. All rights reserved.</p>
  </footer>

  <script>
    function filterCategoryProducts() {
      const search = document.getElementById('categorySearch').value.toLowerCase();
      document.querySelectorAll('#categoryProductGrid .product-item').forEach(card => {
        const name = card.getAttribute('data-name');
        card.style.display = name.includes(search) ? '' : 'none';
      });
    }

    function addToCart(productId, productName, productPrice, productImage, productDescription) {
      if (!localStorage.getItem('token')) {
        alert('Please log in to add to cart.');
        return;
      }
      let cart = JSON.parse(localStorage.getItem('cart')) || [];
      const existing = cart.find(item => item.id === productId);
      if (existing) {
        existing.quantity += 1;
      } else {
        cart.push({
          id: productId,
          name: productName,
          price: parseFloat(productPrice),
          image: productImage || '/images/default-image.jpg',
          description: productDescription,
          quantity: 1
        });
      }
      localStorage.setItem('cart', JSON.stringify(cart));
      alert('Product added to cart!');
    }
  </script>

</body>
</html>
